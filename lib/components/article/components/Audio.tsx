import { ReactElement } from "react";
import { Canvasflow } from "../../../Canvasflow";
// TODO Implement audio
export const Audio = (
  props: Canvasflow.Component.Audio,
): ReactElement | null => {
  console.log(props);
  return null;
};

export default Audio;
