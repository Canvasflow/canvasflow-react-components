import { ReactElement } from "react";
import { Canvasflow } from "../../../Canvasflow";
// TODO Implement gallery
export const Gallery = (
  props: Canvasflow.Component.Gallery,
): ReactElement | null => {
  console.log(props);
  return null;
};

export default Gallery;
