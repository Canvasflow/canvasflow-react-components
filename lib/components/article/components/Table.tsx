import { ReactElement } from "react";
import { Canvasflow } from "../../../Canvasflow";
// TODO Implement table
export const Table = (
  props: Canvasflow.Component.Table,
): ReactElement | null => {
  console.log(props);
  return null;
};
